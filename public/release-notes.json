{
  "version": "0.1.0",
  "notes": "field/drawing: end active drawing when mode toggled off; persist drawing mode without redundant first write; stabilize useGameSessionWithHistory by referencing latest callbacks via refs; add edge-case test"
}