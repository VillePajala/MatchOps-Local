# Environment Variables Example
# Copy this to .env.local and fill in your values

# ============================================================================
# BACKEND MODE CONFIGURATION
# ============================================================================
# Options: 'local' (default) or 'cloud'
# - local: Uses IndexedDB for storage (works offline, no account needed)
# - cloud: Uses Supabase for storage (requires account, syncs across devices)
NEXT_PUBLIC_BACKEND_MODE=local

# ============================================================================
# SUPABASE CONFIGURATION (Required for cloud mode)
# ============================================================================
# Get these from your Supabase project settings:
# https://supabase.com/dashboard/project/_/settings/api
#
# Two Supabase projects exist:
# - Production (matchops-cloud): Real billing, real users
#   URL: https://aybjmnxxtgspqesdiqxd.supabase.co
# - Staging (matchops-staging): Mock billing for testing
#   URL: https://hwcqpvvqnmetjrwvzlfr.supabase.co
#
# For local development, use staging to enable mock purchases.
# See docs/03-active-plans/billing-implementation-plan.md for details.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# ============================================================================
# SENTRY CONFIGURATION (Error Reporting)
# ============================================================================
NEXT_PUBLIC_SENTRY_DSN=your_sentry_dsn_here
SENTRY_AUTH_TOKEN=your_sentry_auth_token_here
SENTRY_ORG=your_sentry_org_here
SENTRY_PROJECT=your_sentry_project_here

# Development Configuration
NEXT_PUBLIC_SENTRY_FORCE_ENABLE=false
ANALYZE=false

# Internal Testing Mode
# Set to 'true' to enable simulated premium purchase/revoke for testing
# Use this for Play Store internal testing track before Play Billing is integrated
# WARNING: Never set to 'true' in production! A safeguard in UpgradePromptModal.tsx
# blocks this in Vercel production regardless, but keep this false for safety.
NEXT_PUBLIC_INTERNAL_TESTING=false

# ============================================================================
# ⚠️  DANGER ZONE - DEVELOPMENT ONLY
# ============================================================================
# Mock Billing Mode - BYPASSES ALL PAYMENT VERIFICATION
# ⚠️  NEVER SET THIS IN PRODUCTION BUILDS
# ⚠️  If set during build, mock purchases will work without real payment
#
# Runtime protection exists in playBilling.ts but build-time is safer.
# Use NEXT_PUBLIC_INTERNAL_TESTING instead for Play Store testing.
#
# NEXT_PUBLIC_MOCK_BILLING=true

# Debug Configuration (Development Only)
# Comma-separated debug categories: home,history,tactical
# - home: HomePage render cycles and state updates
# - history: Undo/redo history operations (main field)
# - tactical: Tactical view undo/redo and drawing operations
NEXT_PUBLIC_DEBUG=
# Enable all debug categories at once
NEXT_PUBLIC_DEBUG_ALL=

# ============================================================================
# REFERENCE: All Project Keys & Credentials
# ============================================================================
# These are NOT used by the app at runtime. This section serves as a
# centralized inventory of all keys and credentials for the project.
# Actual values must be stored in a password manager.
#
# --- Supabase (valoraami@gmail.com) ---
# SUPABASE_SERVICE_ROLE_KEY_STAGING=    # Supabase Dashboard → Settings → API (matchops-staging)
# SUPABASE_SERVICE_ROLE_KEY_PRODUCTION= # Supabase Dashboard → Settings → API (matchops-cloud)
# SUPABASE_DB_PASSWORD_STAGING=         # Supabase Dashboard → Settings → Database (matchops-staging)
# SUPABASE_DB_PASSWORD_PRODUCTION=      # Supabase Dashboard → Settings → Database (matchops-cloud)
#
# --- Resend (villejohannespajala@gmail.com) ---
# RESEND_API_KEY=                       # resend.com → API Keys (used as Supabase SMTP password)
#
# --- Android / TWA Signing ---
# ANDROID_KEYSTORE_PATH=                # /home/villepajala/keys/matchops-release.keystore
# ANDROID_KEYSTORE_PASSWORD=            # Password for the keystore file
# ANDROID_KEY_ALIAS=                    # Key alias (e.g. matchops-key)
# ANDROID_KEY_PASSWORD=                 # Password for the key alias
#
# --- GitHub (valoraami@gmail.com) ---
# GITHUB_TOKEN=                         # github.com → Settings → Developer settings → Personal access tokens
#
# --- Cloudflare (valoraami@gmail.com) ---
# CLOUDFLARE_API_TOKEN=                 # cloudflare.com → Profile → API Tokens (if created)
#
# --- Namecheap (valoraami@gmail.com) ---
# (No API keys needed — managed via web dashboard)