# MatchOps-Local: Unified Project Roadmap

**Last Updated**: 2026-06-10
**Status**: 🚀 **LIVE IN PRODUCTION** (Google Play, released 2026-06-09) · **Free** (no billing)
**Purpose**: Single, prioritized source of truth for remaining work.

> Consolidated and triaged post-launch (2026-06-10). Monetization is parked (going free).
> Completed/scrapped plans were archived to `docs/08-archived/completed-active-plans/`.
> Speculative feature ideas live in `docs/04-features/future-vision.md` — not deleted, just
> off the active roadmap. Preserved code snapshots: git tags `archive/pre-cloud-backup`,
> `archive/planner-integration`, `archive/desktop-responsive-modals`.

Priorities are **P0 (do first) → P4 (someday)**, ordered by what matters most for a free,
solo-maintained, just-launched app: live correctness first, then observability, then UX, then
features, then big bets.

---

## 🔴 P0 — Fix because we're live

Real issues affecting production users / exposure right now. Small, high-leverage.

- [x] **Store-listing accuracy** — ✅ **DONE 2026-06-18**. Live Play listing was already corrected by the user; aligned the stale repo copy: `store-description-en.md` no longer claims "no account / optional cloud / data stays on device" (now: free account required, secure cloud sync, works offline at the field). Added a README note that the Play build is cloud-only (the README's dual-mode description is otherwise accurate for the web/PWA version).
- [x] **Stop publishing internal docs** — ✅ **DONE 2026-06-18** (PR #435). `site/scripts/copy-docs.js` flipped from denylist to **allowlist** — only `QUICK_START.md` + `USER_MANUAL.md` publish; all internal dirs (roadmap/security findings, DB schema, blueprint, analysis, business, archive) are excluded. Also closed the larger surface: the **GitHub repo was public** and is now **private** (user keeps a separate mock/showcase repo).
- [x] **`get_user_consent` returns stale 'granted'** (#371) — ✅ **DONE 2026-06-18**. RPC fix was already LIVE (prod hotfix 2026-06-10 `fix_consent_rpc_ordering`; repo migration 029, PR #422). Repo sync completed: added `user_consents.status` (`text NOT NULL DEFAULT 'granted'`) to `src/types/supabase.ts` Row/Insert/Update; tsc clean. Optional consent hardening (uuid id, month regex from #374/#375) deferred — not blocking.

- [x] **Redeploy delete-account Edge Function** — ✅ **DONE 2026-06-18**. Deployed the refactored repo code (index.ts + handler.ts) as **v10** via the Supabase MCP, `verify_jwt=false` preserved. Before deploying, found the repo had drifted from the safe live behavior on one security-relevant line (rate-limit IP precedence — see CR-M8) and fixed the repo first so the deploy was a true no-behavior-change refactor. Verified live: OPTIONS→200, GET→405, POST-no-auth→401, POST-bad-token→401 (full path incl. rate-limit RPC + JWT verify), identical to the v9 baseline.

### 🧨 Code review 2026-06-11 — Critical (data loss / broken core flows)

Found by a 7-agent whole-app review; top claims hand-verified against code.

- [x] **CR-C1: Mid-game reload corrupts the match clock** — ✅ **DONE 2026-06-12** (PR #424): `RESUME_GAME` resumes loaded in-progress games at the saved clock/period; boot consumes the hidden-session timer record (background time folded in, capped at period boundary) then clears it; orchestration-level tests for the force-reload path. ⚠️ Manual device QA still recommended: start game → lock phone 6+ min → reopen → Start → clock continues. Was: — saved in-progress game loads as `notStarted` (`useGameSessionReducer.ts:500`); pressing Start dispatches `START_PERIOD(1)` which resets elapsed/period and wipes `completedIntervalDurations` (`useGameTimer.ts:27`); boot-time `RESTORE_TIMER_STATE` is dead code (only applies to `inProgress`, which load can never produce — `useGameOrchestration.ts:857`). Common because `useAppResume.ts:178` force-reloads after >5 min background. Auto-save then persists the corrupted clock. Fix: first-class resume path (`RESUME_GAME` from saved time/period when `elapsed > 0`); reconcile `stableStartTime` on `START_PERIOD`.
- [x] **Timer pauses on backgrounding and silently stays paused** (user-reported incident, 2026-06-15) — ✅ **DONE 2026-06-16** (PR #427): in-session background→foreground path (separate from CR-C1's reload path). Old code paused on hide and relied on reading a `wasRunning` marker back from IndexedDB to resume; that read fails on mobile (fire-and-forget write never flushes before freeze, or a no-`wasRunning` tick-save overwrites it) → timer stayed paused, match time lost. Fix: never pause on backgrounding — the precision timer is wall-clock based, so on return re-anchor to `Date.now()` truth and keep running (`usePrecisionTimer.reanchor`); pausing happens only on the explicit control. Follow-ups below.
  - [x] **>5min auto-resume** — ✅ **DONE 2026-06-19** (two parts). **On-device QA of the first attempt (#439) FAILED**: a 6-min lock force-reloaded the app → dropped to the start screen → "Jatka" loaded the game paused. Root cause: `useAppResume` force-reloads unconditionally at >5min, throwing away the live in-game state (and the boot auto-resume didn't fire on the start-screen→Continue path). **Real fix:** suppress the force-reload while a match clock is running (`matchTimerSignal` set by `useGameTimer`, read by `useAppResume`) — the wall-clock reanchor already keeps the timer going, so the user stays on the game screen with the clock running, no reload/Jatka/pause. The force-reload still applies when idle (no live match). The #439 boot auto-resume (`RESUME_GAME` after load, capped at period boundary) remains as a backstop for the rare case the OS itself kills the WebView. Also fixed a latent bug: `RESUME_GAME` was uncategorized in the history wrapper (console.error on every manual resume) — now NO_HISTORY. **Re-QA the >5min lock on device.**
  - [x] **Dead-code cleanup** — ✅ **DONE 2026-06-20**. Removed the orphaned `PAUSE_TIMER_FOR_HIDDEN` + `RESTORE_TIMER_STATE` reducer actions (union members + cases), their NO_HISTORY classification entries, the `useTimerRestore` export from `usePrecisionTimer`, and all their tests/mocks (reducer test, action-validation reference list, usePrecisionTimer test, useGameTimer mock). Confirmed zero production dispatches before removing; 157 timer/reducer/history tests green.
  - [x] ⚠️ **On-device QA** — ✅ **CONFIRMED FIXED 2026-06-20**. Diagnosed via temporary Sentry `timer_diag` instrumentation on the real Android 10 device (could not reproduce locally). Three confirmed-from-data fixes: (1) **durable wall-clock anchor** (PR #444) — async IndexedDB recovery record never flushes before the OS freezes the WebView; replaced with a synchronous `localStorage` anchor that survives freeze/kill (Sentry showed `source:"anchor"`, time recovered correctly). (2) **deferred boot auto-resume** (PR #446) — `RESUME_GAME` was batched with `LOAD_PERSISTED_GAME_DATA`, so `useGameTimer`'s `stableStartTime` never synced and the timer stayed paused; now dispatched one render later via `pendingAutoResumeRef` + effect, mirroring the manual Start-tap flow (Sentry confirmed `timerRunning:true`). Also closed a load re-entrancy race. (3) Diagnostics removed (PR #447). Lock-mid-match → reopen → clock running with correct time, verified on device.
- [x] **CR-C2: Transient IndexedDB read failure → wholesale data wipe** — ✅ **DONE 2026-06-12** (PR #423): all 8 `load*` helpers now propagate storage read errors (only JSON corruption reads as empty); `createTeam` reads both collections before its first write; per-collection abort-without-write regression tests added. Consciously rejected as over-engineering: two-phase commit for createTeam's two writes (orphan self-heals — `getTeamRoster` defaults to `[]`), and `getSettings`/`getWarmupPlan` outer-try (read-only, no RMW follows; `updateSettings` reads storage directly and propagates).
- [x] **CR-C3: Cloud: deleting a season/team/tournament with attached games always fails** — ✅ **DONE 2026-06-11**: applied to prod (`fix_composite_fk_set_null_columns`, verified with rolled-back pre/post probes) + repo migration 028 merged (PR #421). Was: composite FKs from 013 nulled ALL columns incl. NOT NULL `user_id` → 23502 on every referenced-entity delete.
- [x] **CR-C4: Stale-snapshot rollback via re-migration** — ✅ **DONE 2026-06-12** (PR #425): bootstrapGetItem propagates infra errors; migration-completed sentinel in IndexedDB checked before any copy (unreadable sentinel aborts); target-not-empty guard skips the copy and repairs config. Was: — `bootstrapGetItem` swallows read errors as `null` (`storageBootstrap.ts:101`) → config falls back to `mode:'localStorage'` → `performIndexedDbMigration` re-runs on app start and overwrites live IndexedDB with the migration-day localStorage snapshot (kept as backup, `migration.ts:249`). Fix: distinguish read-error from missing-key + "migration done" sentinel stored in IndexedDB itself.

### Code review 2026-06-11 — High

- [x] **CR-H1: Sync lost-update via dedup id reuse** — ✅ **DONE 2026-06-20**. `enqueue` dedup-replaces a still-`pending` op's data under the same id; in the window between `getPending()` (which snapshots `op.data`) and `markSyncing()`, that could swap in newer data (e.g. a 2nd goal logged during a slow push) while SyncEngine still held the stale snapshot — it pushed the old data and `markCompleted()` then deleted the op holding the newer write (lost update). Fix (`SyncEngine.processOperation`): after `markSyncing()` flips status to `syncing` (which stops further dedup-replace, since `enqueue` only matches `pending`), **re-read the op via `getById()` and execute the fresh data**. Regression test added (verified it fails on the stale-snapshot path, passes with the re-read); 168 sync tests green.
- [x] **CR-H2: Account-switch race disposes the live SyncEngine** — ✅ **DONE 2026-06-20**. Stale-init recovery (`factory.ts:732`) closes the OLD store via `instance.close()` → `SyncedDataStore.close()` → `resetSyncEngine()`, which unconditionally disposed whatever the global singleton was — by then the NEW user's engine — leaving their sync dead until reload. Fix: `resetSyncEngine` now takes an optional `engine` ownership guard and only resets when the singleton IS that engine; `SyncedDataStore.close()` passes its owned engine. The factory's deliberate force-close/full-reset calls stay unconditional (not on the stale path). Regression test added (singleton survives a foreign-engine reset, disposes on the owning reset); 169 sync tests green.
- [x] **CR-H3: Offline cold start >1h locks cloud users out** — ✅ **DONE 2026-06-20**. Two fixes: (1) `getCachedUserIdentity` (`cachedSession.ts`) no longer rejects an expired access token when a **refresh token** is present — after >1h offline the access token is expired but the refresh token still recovers the session, so it now grants grace-period access (only rejects expired+no-refresh). (2) The `AuthProvider` init **catch** (where an offline `getCurrentUser()` NetworkError lands) now runs the grace-period check (trigger 4), mirroring triggers 1/2 — a thrown init no longer hard-locks the user at the LoginScreen. Tests added for both (expired-with-refresh → identity; expired-no-refresh → null; init-throws → grace period). 50 auth tests green.
- [x] **CR-H4: initAuth failure strands the user** — ✅ **DONE 2026-06-20**. (1) The init **catch** now sets `initTimedOut` (which drives the cloud retry screen) when it can't recover — previously a thrown init with no cached session left the user stranded (authService unset → `signIn()` returns "Auth not initialized" forever, no UI affordance, e.g. a chunk-load failure post-deploy). It still prefers grace period when a cached session exists (CR-H3 trigger 4); only sets the retry flag when neither applies. (2) The **success path** now clears `initTimedOut`, so a late init success after the 20s safety timeout fired no longer leaves a signed-in user stuck on "Connection Timeout." Tests added (throw+no-cache → retry screen; late success after timeout → cleared). 29 auth tests green.
- [x] **CR-H5: Invisible tactical-ball touch dead zone at center field** — ✅ **DONE 2026-06-12** (PR #426): touch hit-test gated to tactics view like the mouse path; regression tests verified to fail against ungated code. Was: — `SoccerField.tsx:1578`: touch path checks `isPointInBall` unconditionally; mouse path gates behind `isTacticsBoardView`. Default ball pos = kickoff spot; touches there drag the invisible ball instead of the player. One-line fix: gate with `isTacticsBoardView &&`.
- [x] **CR-H6: Async destructive confirms double-fire** — ✅ **DONE 2026-06-20** (all three parts). **Delete-event double-fire**: `handleDeleteGameEvent` (`useGamePersistence.ts`) spliced storage by a React-state index, so a double-tapped confirm ran a second time with a stale index → deleted the WRONG event + decremented the score twice. Added an in-flight guard (rejects a concurrent repeat for the same event id), so the single deletion uses a fresh index; also wired `isConfirming` into all three delete-event ConfirmationModals (GoalLogModal, GameSettingsModal, GameStatsModal/useGoalEditor) so the confirm button disables after the first tap. Regression test: concurrent duplicate delete removes storage + dispatches score change exactly once. ✅ **Restore double-run DONE 2026-06-20** (CR-H6b-1): `importFullBackup` now has a module-level in-flight guard (rejects a concurrent restore so two clear+write cycles can't interleave) and `SettingsModal`'s restore confirm passes `isConfirming` (button disables after first tap). Regression test: a concurrent second restore returns null and `clearAllUserData` runs once. ✅ **Restore rollback DONE 2026-06-20** (CR-H6b-2): `importFullBackup` clears all data before writing, so a write failure after the clear used to be total data loss. Now it snapshots all current data via `generateFullBackupJson` BEFORE the clear, and on write failure recovers the original data from that snapshot (recovery only ADDS data back via a dedicated `restoreSnapshotEntities` writer — no second destructive clear — and retries; the forward path is untouched to avoid regressions). User sees a precise message ("original data was recovered" / "recovery did not complete"). Failure-injection regression test verifies the original data is restored when the forward write throws after the clear.
- [x] **CR-H7: Auto-save wipes undo + full snapshot save every second** — ✅ **FULLY DONE 2026-06-22** (remainder): silent auto-saves no longer call `resetHistory` (it ran on every auto-save → the undo stack was wiped each time, so mid-match undo could never reach more than the single most-recent change). `resetHistory` now runs only on an explicit (manual) save = clean baseline. Also replaced the per-save `invalidateQueries` (which refetched storage on every save) with `setQueryData` writing the just-persisted snapshot — the `savedGames` queryFn is a plain storage read, so this keeps query consumers fresh without a refetch on every change. Regression tests: silent save does NOT reset history / does NOT invalidate (uses setQueryData); manual save DOES reset history. ✅ **Per-second save/sync DONE 2026-06-20** (user-reported: cloud-sync icon flickered every second during a match): `timeElapsedInSeconds` was in the immediate (0ms) auto-save tier, so every clock tick fired a full save + cloud sync (~3000/game). Swapped it for `isTimerRunning` in the change-detection set, so the clock saves when the timer **stops/starts** (both `PAUSE_TIMER` and `END_PERIOD_OR_GAME` set the precise `timeElapsedInSeconds` + `isTimerRunning:false`), and goals/scores still save instantly; the save still persists the exact elapsed. Live-clock crash recovery is covered by the timer-state record + localStorage anchor. Regression test added. **Still open:** `resetHistory` on every silent auto-save (`useGamePersistence.ts`) wipes undo; and per-save `invalidateQueries`. Fix: no resetHistory on silent saves; drop per-save invalidation.
- [x] **CR-H8: IndexedDB recovery latches wedge the app until reload** — ✅ **DONE 2026-06-20** (all three parts). **(a)+(b)**: (a) `indexedDbKvAdapter.close()` reset `connectionTerminated` only inside `if (this.db)`, but the `terminated()` callback nulls `this.db` first → the reset was unreachable and the adapter was permanently wedged (`initialize()` always threw "connection terminated and cannot be reused"). Now `close()` resets all connection state unconditionally, so a fresh `initialize()` recovers. (b) `storage.ts` `canRetryNow()` returned false forever after `MAX_RETRY_ATTEMPTS` (retry state only reset on a successful creation, unreachable while locked) → every storage access threw "temporarily unavailable" until reload. Added a TTL-based lockout reset (`maybeResetRetryLockout`, 30s cooldown > max backoff) so a wedged adapter recovers without a reload. Regression tests for both (terminated→close→reuse; lockout→cooldown→recovery). ✅ **(c) DONE 2026-06-20** (CR-H8c): legacy→user-DB migration idempotency was `existingPlayers.length > 0` — and since players are written first, a partial migration that wrote ≥1 player latched into `already_migrated` forever, so games/seasons never arrived. Replaced with a RESUMABLE, NON-DESTRUCTIVE design: it now fetches existing ids per entity type and migrates only what's MISSING (skip-if-exists), so a later run completes the rest and never overwrites an entity the user edited after a partial migration. Settings (a no-id singleton) are written only on a fresh migration. Returns `already_migrated` only when genuinely nothing is missing. Regression tests: partial migration resumes (missing games/seasons migrate, existing player not re-written); an existing/edited entity is never overwritten by stale legacy data.
- [x] **CR-H9: Marketing consent re-grant permanently impossible** — ✅ **DONE**: was already fixed in prod by the 2026-06-10 `fix_consent_rpc_ordering` hotfix (`ON CONFLICT DO UPDATE` bumps `consented_at`); repo synced via migration 029 (PR #422, 2026-06-11).
- [x] **Test-suite cumulative memory leak → CI "Test" job OOM** (found 2026-06-17, PR #431) — ✅ **DONE 2026-06-18**. Root cause: `src/setupTests.mjs` registered an **anonymous** `uncaughtException` process listener at module scope (line ~159) and never removed it. Jest re-runs the setup file per test file in the same worker, so the listener accumulated (~one per suite), and each new one closed over the growing array of prior listeners — quadratic closure retention. Its sibling `unhandledRejection` was correctly paired (add/remove); `uncaughtException` was not. Fix: made the handler a named, module-scoped function and removed it in the same `afterAll` (preserving prior behavior). Verified: full single-process (`--runInBand`) run, which previously OOM'd even at 8 GB, now completes 230/230 suites / 4,860 tests at **~922 MB peak** with **zero** `MaxListenersExceededWarning`. The omitted Unknown-scorer edit round-trip test was re-added. Things tried first that did NOT help (kept for reference): raising `--max-old-space-size` to 6/8 GB; `workerIdleMemoryLimit` at 1 GB/512 MB.

### Code review 2026-06-11 — Medium

- [x] **CR-M1 (sync correctness cluster)** ✅ **ALL 6 ISSUES DONE 2026-06-22** (PRs #463–466 + this): ~~failed/syncing ops escape dedup and later replay stale snapshots (`SyncQueue.ts:329`, `retryFailed:1163`)~~ ✅ **Issue 1 dedup-escape FIXED 2026-06-22**: `enqueue` now merges a new edit into a `pending` OR `failed` op (was pending-only), so a terminal failed op's stale snapshot can't survive as a duplicate and later replay over the newer write (e.g. via the user's "retry failed"). In-flight `syncing` ops are still never touched (owned by the executor). The `retryFailed` timing (clearing `lastAttempt`) is intentional — it's only called from the user's "retry" button (`useSyncStatus.ts:369`), where immediate retry is the desired UX, so left as-is. Regression test: a fresh edit merges into a failed op (same id reused, reset to pending, newest data wins). ~~conflict resolver swallows name-collision unique violations as "already synced" and acks without pushing (`conflictResolution.ts:236`)~~ ✅ **Issue 2 FIXED 2026-06-22**: `handleMissingCloudRecord` now re-fetches BY ID after an already-exists error — if our id is present it's a genuine id race (ack, as before); if our id is still absent it was a name/unique collision on a DIFFERENT record, so it throws (the op fails/retries and the conflict is visible) instead of silently dropping the local entity. Tests: id-race → success; name-collision → surfaced. (`handleLocalWrite` already had the record by id, so it's unaffected.); LWW compares device clock vs server clock — skewed phone loses fresh edits (`conflictResolution.ts:185` vs `001_rpc_functions.sql:79`); ~~`pause()` isn't a barrier — in-flight op can write into a just-cleared cloud (`SyncEngine.ts:403`, `SyncedDataStore.ts:1069/1459`)~~ ✅ **FIXED 2026-06-22**: added `SyncEngine.waitForIdle()` (polls `isSyncing`) and made the batch loop break on `isPaused`; `pushAllToCloud`/`clearAllUserData` now `pause()` then `await waitForIdle()` BEFORE clearing, so an in-flight write can't land in a just-cleared store. Resume is now guarded by whether WE paused (don't un-pause an engine an outer op had paused) — closes the pause-state leak too. Barrier-ordering + idle tests added; ~~upsert* enqueues 'create' → CREATE+DELETE coalescing can cancel a needed delete (`SyncQueue.ts:429`)~~ ✅ **FIXED 2026-06-21**: the six `upsertX` methods in `SyncedDataStore` now enqueue `'update'` instead of `'create'`, so a later DELETE coalesces UPDATE+DELETE→DELETE (reaches the server) instead of CREATE+DELETE→null (silently dropped). The cloud executor maps both create/update to the same `upsert*` call, so op type only affects queue coalescing — no behavior change for the push itself. Genuine `createX` methods correctly keep `'create'`. Test updated; SyncQueue UPDATE+DELETE→DELETE already covered. ~~pause-state leaks in clearAllUserData/pushAllToCloud (`SyncedDataStore.ts:1447-1497`)~~ ✅ **FIXED 2026-06-22** (with Issue 4 above — resume now guarded by `wasPaused`). ~~clock-skew LWW (Issue 3)~~ ✅ **FIXED 2026-06-22**: conflict resolution now biases toward the local edit — `timestamp >= cloudTimestamp - CLOCK_SKEW_TOLERANCE_MS` (5 min) in both `handleLocalWrite` and `handleLocalDelete`, so a drifted device clock can no longer silently discard a fresh local edit; the cloud only wins when it is newer by more than the tolerance. (User-chosen strategy: prefer-my-latest-edit.) Tests: within-tolerance local-wins + beyond-tolerance cloud-wins. _Remaining M1: dedup escape + retry timing (Issue 1); AlreadyExists swallow (Issue 2 — has documented rationale; needs its own investigation of cloud writeToCloud error semantics before changing)._
- [x] **CR-M2 (transform fidelity)**: ✅ **DONE 2026-06-21.** (1) `transformSettingsFromDb` now carries `updated_at` → `updatedAt` so settings conflict resolution has a real timestamp to compare (previously always resolved local-wins). (2) `transformPlayerToDb`/`transformSeasonToDb`/`transformTournamentToDb` now preserve the original `created_at` on sync push / upsert (`created_at: entity.createdAt ?? now`) instead of hard-resetting it to `now` every push — matches the teams/personnel pattern, ends ordering churn, and stops destroying original creation timestamps. Regression tests: settings `updatedAt` round-trip; player/season/tournament upsert payloads preserve `created_at`.
- [~] **CR-M3 (optimistic locking weaker than tests suggest)** — mostly **FIXED 2026-06-22** (migration `030`, verified live on staging via the REST RPC): (1) ✅ version SELECT now uses `FOR UPDATE` (closes the TOCTOU race); (2) ✅ **discovered via staging testing** — conflicts were raised with `serialization_failure` (40001), which PostgREST auto-retries as transient → the conflict request timed out (HTTP 504) and **never surfaced to the user**; now raised with `PT409` → returns HTTP 409 immediately (app conflict-detection + both retry modules updated to recognize `PT409`, keeping `40001` for the pre-deploy window); (3) ✅ cold `gameVersionCache` now fetches the current cloud version and passes it (instead of NULL = skip-check → unguarded overwrite). **DEPLOY NOTE: migration 030 must be applied to PROD Supabase** for the conflict/lock fixes to take effect (applied to staging only). _Deferred:_ ambiguous-network retry of an already-committed save can still throw a spurious ConflictError (`:3571-3652`) — rare (needs a network blip in the exact post-commit/pre-response window), not data loss (the save landed; refresh shows it), and a safe fix needs careful own-write-vs-real-conflict detection that must not mask real conflicts.
- [x] **CR-M4 (UI)**: ✅ **league-filter data-loss DONE 2026-06-20** — browsing league FILTERS instantly persisted `leagueId: undefined`, so just looking around destroyed the saved league. Filters are now pure browse controls (only narrow the visible list, never clear/persist), and the league picker always renders the saved league as an option even when the current filters would exclude it (so the selection never "disappears"). Regression test added. ✅ **Remaining CR-M4 DONE 2026-06-21:** (1) period-duration input now edits a local string buffer and commits a clamped (1-999) value on blur only, so intermediates like "125" when changing 15→25 are never persisted/synced (`GameSettingsModal.tsx`); (2) GoalLogModal goal-edit now reverts the optimistic local + parent update on every persistence-failure branch (mirroring its GameSettingsModal twin) and validates MM:SS bounds (minutes 0-120, seconds 0-59) instead of just `isNaN` (`GoalLogModal.tsx`); (3) goalie toggle no longer double-fires on touch — `preventDefault()` on the shared handler stops the synthetic click after `touchEnd` (`PlayerDisk.tsx`); (4) assister select is now always controlled (`value={editGoalAssisterId ?? ''}`) in both GoalLogModal and GameSettingsModal, fixing the controlled↔uncontrolled flip warning.
- [x] **CR-M5 (auth)** ✅ **5 of 6 DONE 2026-06-22** (#1 deliberately skipped): _#1 (open, by decision)_ suppressed `signed_out` → zombie session after real server-side revocation, sync silently dead (`AuthProvider.tsx:307`) — rare + recoverable (self-heals on cold start), and loosening the logout guard is the only change in this cluster that could itself cause lockouts, so intentionally left; ~~auth listener can leak per retry cycle + registered after long awaits (`:256-267`)~~ ✅ **#2 FIXED 2026-06-22**: `onAuthStateChange` was subscribed AFTER the init awaits (incl. the marketing-consent fetch), so events in that window were missed and an interrupted/re-run init could orphan the subscription (leak / double-handling). The subscription is now registered BEFORE the consent await (assigned before any interruptible await, so the effect cleanup reliably unsubscribes it); the consent fetch moved after. Test: exactly one listener after init, unsubscribed on unmount. ~~grace-period trigger 3 keeps a non-null session, violating the documented invariant (`:332`)~~ ✅ **#3 FIXED 2026-06-22**: grace period is supposed to mean "user kept from cache, no live session" (`isAuthenticated` falls back to `isAuthGracePeriod`), but trigger 3 (mid-session offline signed_out) and the init-error/timeout grace paths left the stale session object in state — feeding a dead token to callers. All grace-period entries now `setSession(null)`. Because `isAuthenticated` uses `|| isAuthGracePeriod`, clearing the session can't log the user out. Test: trigger 3 enters grace period, stays authenticated, session cleared. ~~reload mid password-reset signs user in with OLD password (`SupabaseAuthService.ts:1222`)~~ ✅ **#4 FIXED 2026-06-22**: the recovery session from `verifyOtp(recovery)` persists in storage, but the "don't treat as sign-in" guard was in-memory only — a reload mid-reset lost the guard and auto-signed-in (password never changed). Added a persisted `matchops-password-reset-in-progress` flag (set when the recovery OTP verifies, cleared on password update / abandonment); on init, if set, the lingering recovery session is signed out so the user lands on login (their password is unchanged; they can sign in or restart the reset). Self-healing: the flag is cleared on init regardless, so it can't affect a future normal session. (Recovery sessions are indistinguishable from normal ones after reload, so detection-on-init wasn't viable — the persisted flag is the reliable signal.) Test: flag + recovery session on init → signed out, not authenticated, flag cleared. ~~re-consent race via SIGNED_IN event path (`:344`)~~ ✅ **#5 FIXED 2026-06-22**: `signIn()` checks consent and sets `needsReConsent` BEFORE user/session, but `authService.signIn()` fires the SIGNED_IN event, and `onAuthStateChange` would set user/session during signIn's consent `await` — briefly showing the app logged-in before the re-consent modal. Added an `isInteractiveSignInRef`: while an interactive `signIn()` runs, the auth-event handler defers user/session setting to `signIn()` (which orders `needsReConsent` first); the ref is cleared in a `finally`. Cache-clearing in the handler still runs. Test: a SIGNED_IN event fired during the consent await does not log the user in before re-consent resolves. ~~AbortError fallback validation can wipe a valid recovered session (`SupabaseAuthService.ts:328-401`)~~ ✅ **#6 FIXED 2026-06-22**: the `getUser()` validation cleared the session whenever it RETURNED an error, but on flaky mobile (Chrome AbortError) that returned error is "couldn't reach the server", not "server rejected the session" — so a valid session was wiped (spurious logout). Now a returned network/abort error (`isNetworkError`) trusts the cached session (consistent with the catch block); only a definitive rejection (401/403, or no-user with no network issue) clears it. Regression test added. _Plan: do #6 (done), #2 (listener leak), #3 (stale grace-period session), #5 (re-consent race); #4 (password-reset reload) only if recovery sessions are cleanly detectable; **#1 (zombie session) deliberately skipped** — rare + recoverable, and loosening the logout guard is the only change here that could itself cause lockouts._
- [x] **CR-M6 (data-layer parity)** _(real bugs fixed; only low-value/inherent remnants, won't-do)_: ~~`getTournamentReferences` dead series clause (`LocalDataStore.ts:2455`)~~ ✅ **FIXED 2026-06-22**: the games filter's second clause re-checked `tournamentId` (dead code), so a game linked to a tournament ONLY via a series didn't block deletion → orphaned game on delete. Now tests `seriesIds.has(g.tournamentSeriesId)`, matching the adjacent teams check and SupabaseDataStore (which was already correct). Regression test added. ~~**composite-uniqueness `clubSeason` divergence**~~ ✅ **FIXED 2026-06-22**: not a config-philosophy decision after all — `getSeasons()` (the UI's source) already computes `clubSeason` only when missing, so the canonical value is well-defined. The real divergence: Local's `createSeason`/`updateSeason` duplicate checks read `loadSeasons()` (RAW rows, blank `clubSeason` for legacy data) while Supabase reads `getSeasons()` (computed-when-missing). So a legacy blank-`clubSeason` season was treated as distinct in Local but a duplicate in Cloud. Local now computes `clubSeason ?? calculateClubSeason(startDate, start, end)` in both checks, matching getSeasons/the UI/Cloud. Regression test: a legacy season with a startDate but no stored clubSeason is now correctly a duplicate. _Remaining M6 (low-value / inherent): **`updateSettings` non-atomic + skips validation** — low-risk for single-user per Rule 17 (PostgreSQL handles concurrency); **`deleteWarmupPlan` return divergence** (Local always true, Supabase count>0) — low-value, return value rarely consumed; **one-way gameTime/gameDate loss on migration** — inherent to a non-null DATE column (invalid input must default; a warning is already logged)._
- [x] **CR-M7 (storage/migration)** _(fully complete)_: ~~migration "success" with up to 50% keys lost — core keys must be mandatory (`migration.ts:230`)~~ ✅ **FIXED 2026-06-22**: the localStorage→IndexedDB copy marked the migration "completed" whenever ≥50% of keys transferred, even if core user-data keys (games, roster, seasons, tournaments) failed — and since the app reads IndexedDB afterward, a missing core key was silent data loss. Now any failed CORE key throws (→ migration recorded "failed", not "completed"), independent of the overall success rate. Regression test added. app-data migration reads roster from IndexedDB BEFORE the localStorage copy runs → empty default team for v1 upgraders (`migration.ts:119`); ~~mutex released by non-owner on acquire timeout (`storage.ts:309/704`)~~ ✅ **FIXED 2026-06-22**: both adapter-creation mutex sites (`getUserStorageAdapter`, `getStorageAdapter`) called `release()` in `finally` even when `acquire()` threw (timeout/queue-full) — but `release()` only no-ops on a fully-free mutex, so a post-timeout release stole the *current owner's* lock and broke mutual exclusion. Both sites now track an `acquired` flag and only release what they actually acquired. Added a MutexManager test documenting the hazard; legacy `savedGames` backups skip player remapping due to normalization ordering (`fullBackup.ts:574/612`); ~~SW caches every navigated HTML URL with no trim (`sw.js:121`)~~ ✅ **FIXED 2026-06-22**: the document handler now caches/matches by a query-stripped key (`origin + pathname`) instead of the full request URL, so infinitely-varying query strings for one route no longer each create a cache entry (routes are finite → bounded). Test asserts the normalized key. ~~_Remaining M7: v1-upgrader empty-team ordering; legacy `savedGames` backup player-remap ordering._~~ ✅ **FIXED 2026-06-22**: (a) `runMigration` ran the app-data migration (which reads the master roster via the IndexedDB-only adapter) BEFORE the localStorage→IndexedDB copy, so v1 upgraders got a default team with **no players** (roster read from an empty IndexedDB). Reordered: the IndexedDB copy now runs first. (b) `importFullBackup` ran the player-remap step before the legacy-key normalization (`savedGames` → `savedSoccerGames`), so a legacy backup's games were invisible at remap time and kept stale player IDs. Normalization now runs first. Both covered by regression tests (call-order assertion; legacy-key import produces a mapping report). **CR-M7 fully complete.**
- [~] **CR-M8 (backend, dormant until billing/consent matters)** _(consent RLS fixed + live; rest is billing-dormant)_: purchase-token reuse race — no unique index on token (`019` is non-unique, `verify-subscription/index.ts:283`); `is_active` never expires without a client call (`010:96`); purchase never acknowledged with Google → auto-refund in ~3 days when billing goes live; ~~`user_consents` directly writable via PostgREST `FOR ALL` policy — replace with SELECT-only like the 018 subscriptions fix (`016:99`)~~ ✅ **FIXED 2026-06-22** (migration `031`, applied to staging + **PROD** 2026-06-23, live-verified): migration 016 widened the consent policy to `FOR ALL`, so an authenticated user could directly INSERT/UPDATE/DELETE their own `user_consents` rows via PostgREST — forging a `granted` they never gave, or deleting a withdrawal, corrupting the GDPR consent ledger. Reverted to `FOR SELECT`; all consent RPCs (`record_/revoke_/get_user_consent`, `get_marketing_consent_status`) are SECURITY DEFINER and bypass RLS, so the app's reads/writes are unaffected. Live test: direct INSERT → `42501` (blocked), direct DELETE → 0 rows (row survives), RPC write + direct SELECT → still work. `check_rate_limit` cleanup uses caller's window — latent until windows diverge (`024:46`); ~~`cf-connecting-ip` trusted before `x-forwarded-for` in rate-limit keys (both edge functions)~~ ✅ **FIXED 2026-06-18**: both `delete-account/handler.ts` and `verify-subscription/index.ts` now prefer the platform-set `x-forwarded-for` over the client-forgeable `cf-connecting-ip`, so the rate-limit key can't be rotated to bypass the limit. (delete-account fix is live in edge v10; verify-subscription fix ships to repo and applies on its next deploy.)

- [x] **CR-M9 (goalie marking system — hardening)**: ✅ **DONE 2026-06-21.** Full system investigated and hardened. **Policy decided (user):** the explicit goalie button and drag-into-keeper-spot both work, but position changes are now **non-destructive** — dragging a player INTO the keeper spot promotes them (clearing any other goalie for the single-goalie invariant), and moving players around **never unsets** a goalie (only the button clears one). All goalie changes now route through one authoritative setter (`applyGoalieStatus`) that updates BOTH `availablePlayers` and `playersOnField` and persists, so position-driven promotions can no longer write a two-goalie state to storage. **Fixes shipped:** (1) auto-position override removed — `promoteGoalieByPosition` only promotes, never demotes (`useFieldCoordination.ts`); position promotions notify the parent via an `onAssignGoalieByPosition` bridge so the roster array + storage stay consistent; (2) concurrent-toggle race closed with an in-flight guard on `applyGoalieStatus`; (3) deletion cascade — deleting a roster player now also removes them from `playersOnField` and the game's `selectedPlayerIds` (no orphaned player/goalie), gated on `handleRemovePlayer` returning success (signature now returns `boolean`); (4) single-goalie normalization on load — `normalizeSingleGoalie` keeps the first goalie and clears extras for legacy/edited/migrated saves. Substitution survival verified (off-field goalie keeps its flag in `availablePlayers` and re-drop restores it). Tests added: non-destructive move (no unset), swap-into-keeper promotion, `normalizeSingleGoalie` unit tests, and the roster boolean-return contract. _(Original findings, for reference:)_ full system investigated 2026-06-21. The touch double-fire is fixed under CR-M4; these deeper consistency gaps remain. `isGoalie` lives in three places (master roster, `availablePlayers`, `playersOnField`) and is intentionally per-game field state, not roster-synced (`useGameState.ts:81`). Single-goalie is enforced only at manual-toggle time (`useGameOrchestration.ts:1668` un-goalies others), NOT at load/import — a hand-edited or migrated game with two goalies stays inconsistent. **Gaps to fix:** (1) **substitution loss** — removing a field player (`useFieldCoordination.ts:423`) doesn't reconcile goalie status between `playersOnField` and `availablePlayers`; subbing a goalie off then on can drop the flag (display-only merge in `playersWithFieldGoalieStatus` `:482` doesn't persist). (2) **deletion cascade** — deleting a goalie from the master roster while they're on field in an active game leaves an orphaned `isGoalie=true` player in `playersOnField` (no cascade). (3) **auto-position vs manual toggle ambiguity** — `updateGoalieStatusByPosition` (`useFieldCoordination.ts:350`) silently overrides a manual goalie flag after any drag/swap based on the (0.5,0.95)±0.05 keeper zone; decide which wins and document it. (4) **concurrent toggle race** — `handleToggleGoalieForModal` reads `availablePlayers` from closure; two fast toggles before the first save completes are last-write-wins (storage fresh-fetch mitigates corruption but not the lost in-memory mutation). **Test gaps:** no coverage for multi-goalie prevention, substitution flag survival, deletion cascade, or auto-position-vs-manual conflict (only happy-path persist + display tests exist). Save/load, undo/redo, import, and Excel export all preserve `isGoalie` correctly (verified, no action).

**Verified solid (no action):** RLS on every table; `save_game_with_relations` (027) pins search_path + force-injects user_id/game_id; all 19 transform rules hold in both stores; reducer score/event atomicity; IndexedDB transaction usage; sign-out hygiene; edge-function JWT verification + fail-closed rate limiting; SW/manifest pipeline.

---

## 🟠 P1 — Observability (we just launched — get eyes on it)

- [x] **In-app update banner showed the same text across deploys** (reported 2026-06-19) — ✅ **RESOLVED 2026-06-19.** Investigation: NOT a regeneration/cache bug — `build` runs `generate-changelog.mjs` → `public/changelog.json` regenerates from `release-notes.json` per deploy; the client fetches it network-only with a `?t=` cache-bust (verified live: both prod domains served the new note within minutes of PR #439). The text looked static only because the in-between PRs were `skip-release-notes` internal changes that didn't touch the notes. **Decision (one concept):** every merge to master ships a new build, so every build must carry a user-friendly note. **Removed the `skip-release-notes` escape hatch** — the Release Notes Guard now requires *every* PR to master to add a fresh top entry to `release-notes.json` (EN + FI), no exceptions; deleted the label. The guard verifies a genuinely **new top entry** (compares `releases[0]` against base, not just "file touched"), so editing the comment or tweaking an old entry can't satisfy it. The banner shows that entry. (Future nicety, not required: accumulate all notes newer than the user's installed version so a multi-version skip shows the full list.)
- [x] ~~**Sentry alerting**~~ — **dropped 2026-06-24** (decided not needed). Sentry already captures errors and routes notifications to alerts@velomoai.com; proactive alert rules aren't worth setting up for a free, solo-maintained app at this scale.
- [x] **Reduce Sentry storage-retry noise** — ✅ **FIXED 2026-06-23**: the four IndexedDB retry loops (`getStorageItem`/`setStorageItem`/`removeStorageItem`/`clearStorage`) and the adapter-creation catch logged `error` (→ Sentry) on *every* attempt, so a recovered transient failure still created Sentry issues. Now non-final attempts log `warn` (console only) and only the final failure — the one that throws (loops) or exhausts `MAX_RETRY_ATTEMPTS` and locks out (adapter creation) — logs `error`. The adapter-creation Sentry breadcrumb level is likewise `warning` until the final attempt.
- [x] **CI audit gate** — ✅ **DONE 2026-06-23**: the CI `security-scan` job now has a gating step `npm audit --omit=dev --audit-level=critical` (no `continue-on-error`) that fails the build on CRITICAL vulnerabilities in **production** dependencies. Scoped tightly on purpose: `--omit=dev` ignores build/test tooling and `--audit-level=critical` ignores high/moderate/low (still printed by the existing informational audit step, handled as routine maintenance). Currently 0 critical prod vulns, so it passes; it blocks only future criticals.

---

## 🟡 P2 — UX & quality polish

- [~] **Play Store data-deletion declaration** (deferred 2026-06-11) — Data Safety form wrongly says the app offers no data deletion; full account deletion exists (Settings → Delete Account → `delete-account` Edge Function). Steps: 1) ✅ **verify deletion flow E2E DONE 2026-06-23** — verified live on staging with a throwaway account: `clear_all_user_data` (edge step 1) removed all data rows (games/players/seasons/tournaments → 0), and deleting the auth user (edge step 2) removed the account + cascade-deleted remaining rows (`ON DELETE CASCADE`). Edge-function wrapper is prod-verified (v10); app wiring unit-tested; on-device wipe is the deleteUserLocalDatabases fix. **2) (USER) add `/delete-account` page to marketing site** (Google wants a web deletion-request URL); **3) (USER) update Data Safety form in Play Console.**
- [~] **Local device-data wipe on account deletion** (added 2026-06-11) — ✅ **CORE DONE 2026-06-23**: `AuthProvider.deleteAccount` now calls `deleteUserLocalDatabases(userId)` after the cloud+auth deletion, best-effort-deleting the user-scoped data mirror (`matchops_user_{id}`) AND the sync-queue DB (`matchops_sync_queue_{id}`) — honoring erasure and removing the otherwise orphaned, unreachable mirror. Deliberately leaves the legacy local-mode DB (`MatchOpsLocal`) intact (standalone local-first data, independent of the cloud account). Best-effort/non-blocking (resolves on success/error/blocked + timeout); the post-deletion sign-out teardown closes the adapter so the delete completes. Helper unit-tested (both DB names, blocked/error/throw/empty, legacy-DB-untouched). ✅ **KEEP-DATA AFFORDANCE DONE 2026-06-23**: the delete-account screen now warns that on-device data is erased too (updated copy, EN+FI) and offers an inline **"Export backup first"** button (calls the existing backup export) so a user who only wants out of cloud doesn't lose data — establishing "Delete = erase, keep-data = a separate explicit step." _Remaining (DEFERRED — see cloud-first decision below):_ (b) **explicit "delete all local data"** for no-account local users; (c) the reverse-migration "delete cloud account" path should mirror the wipe.

- [x] **Decision: cloud-first onboarding (local mode frozen, not removed)** — ✅ **2026-06-23**. After weighing keep vs drop vs freeze, chose **soft-B / freeze**: the no-account local-mode **entry points are now hidden for new users on web** (WelcomeScreen `hideLocalModeOptions` + LoginScreen "continue without an account"), matching the Play build — driven by a single `hideLocalModeEntry` flag (`isCloudAvailable() && NEXT_PUBLIC_INTERNAL_TESTING !== 'true'`; when cloud is unconfigured, local stays the only option). **Local mode is hidden, NOT removed:** existing local users bypass onboarding and keep working; the in-app "Enable Cloud Sync" upgrade stays; the code is re-enabled for QA / future revival via `NEXT_PUBLIC_INTERNAL_TESTING`. This stops the dual-mode complexity from growing (no new local users; the deletion follow-ups (b)/(c) and "switch to local" are deferred as won't-do-while-frozen) without a risky teardown. _Follow-ups when convenient:_ align the marketing site (still advertises no-account/on-device), and a later deliberate code teardown if local mode is never revived._
- [x] **Core accessibility — scoped down (decided 2026-06-24)** — No compliance driver (niche, single-user, not public-sector; Play doesn't gate on a11y). ✅ Did the genuinely-useful **contrast pass**: bumped faint `slate-500` info text → `slate-400` in TimerOverlay (play-time history), ShootoutModal, and LoadGameModal (readable in sunlight). **Held:** enlarging tap targets — the ~40px buttons work and enlarging risks layout crowding (esp. the ControlBar row); revisit only if mis-taps become an actual annoyance. **Skipped:** keyboard nav + screen-reader (≈zero users for a touch-only niche coaching tool).
- [x] **Season League UX filters** — ✅ **Already implemented** (roadmap was stale). `SeasonDetailsModal` has **area** + **level** filter dropdowns (`LeagueAreaFilter`/`LeagueLevelFilter`) driving a `filteredLeagues` list instead of one flat 34-league list; filters auto-set to the existing league when editing. Metadata + tests in `config/leagues.ts`.
- [x] **Site polish** — ✅ **DONE 2026-06-23**: added the missing `features.foundation.*` subtree to `site/public/locales/fi/common.json` (FI now at parity with EN — `title` + `offline`/`private`/`backup`/`i18n` `{title,desc}`; descs mirror EN's empty values); fixed `site/README.md` (Next.js 15 → 16, matching `site/package.json` 16.0.10).
- [ ] **Deploy `verify-subscription` edge function** (deferred — billing) — the repo carries the CR-M8 rate-limit fix (prefer `x-forwarded-for` over `cf-connecting-ip`) but it has never been deployed. The function is dormant while billing is parked, so there's no live benefit now. **When billing is revived** (or next time that function is touched), deploy it the same careful way `delete-account` v10 was done: diff deployed-vs-repo, preserve `verify_jwt`, deploy, then smoke-test. Also fold in the other CR-M8 billing items at that time (purchase-token unique index, `is_active` expiry, Google purchase acknowledgement).
- [x] **PR #431 review nits** (Minor, from claude-review) — ✅ **DONE 2026-06-18**. 1) `handleRecalculateScoreFromEvents` (`useTimerManagement.ts`) now depends only on `homeOrAway` + `gameEvents` (the fields `computeScoreFromEvents` reads), so its reference is stable across timer ticks. 2) CSV "Unknown" label — **closed as won't-fix**: `exportCsv` is intentionally all-English (40+ hardcoded literals, takes no `translate`); i18n-ing one word would make it the *only* translated string and more inconsistent, not less. The structural choice (CSV English-only vs Excel i18n) is out of scope for a nit.

---

## 🟢 P3 — Feature backlog (value/effort ordered)

Low-effort, high-value first. Detailed concepts in `docs/04-features/future-vision.md`.

| Feature | Effort | Note |
|---------|--------|------|
| Player Milestones & Certificates | Low | Auto-detect 10/25/50/100 appearances; exportable. High youth-motivation value. |
| Quick Post-Game Ratings | Low | Largely covered by the existing assessment system (`PlayerAssessmentCard`/`AssessmentSlider`); revisit only if a faster 30s flow is wanted. |
| ~~Configurable Formations~~ | — | ✅ Already implemented (`FormationPicker` + `formationPresets.ts`); roadmap was stale. |
| ~~Overtime & penalty shootout (#273)~~ | — | ✅ **DONE 2026-06-24** (PRs #502–512). OT needs no timer — extra-time goals are logged as normal goals (fold into the score). Penalties: free-form per-kick `ShootoutModal` (your shooters named, opponents anonymous; no fixed format), result is **kicks-as-truth** via shared `resolveGameResult` (a recorded shootout always resolves to a Win/Loss across stats + Excel); `shootout_kicks jsonb` (migration 032, staging+prod). Reachable from the **timer overlay** (always visible) and Game Settings, with a first-kick/"up next" guide and instructions. Persisted via the immediate auto-save tier. Future: format presets + deep per-player/keeper shootout stats; a **timed-OT clock** is gated behind the timer-hardening refactor (P4). Plan: `overtime-penalty-shootout-plan.md`. |
| ~~Substitution "who came off" field (#381)~~ | — | **Reframed → P4** "Playing-Time Fairness & Multi-Game Sub Planner" (`playing-time-fairness-and-planner.md`). Real-time sub-event logging fights the app's purpose; the real need is fair *playing time* across contexts, captured post-game. |
| Formation Templates (one-tap switch) | Med | Save/switch formations; ends 11-player drag pain. |
| Moment Capture (tap-to-log) | Med | 2-tap logging of notable plays; goals/assists miss most player value. |
| Field Export (image / PDF) | Med | Shareable field + lineup. |
| Futsal Field Visualization | Med | Completes existing futsal support (smaller court, 5 players). |
| Visual Analytics | Med | Charts, event timeline, goal-log filtering. |
| Age-appropriate assessment profiles (#364) | Med | U8-U10 simplified, U13+ full. Aligns with youth focus. |
| Game captains | Low | Record a captain (or captains) per game — not tracked at all today. Could surface in game info + per-player "times captain" stat. Idea captured 2026-06-24. |
| Position history (per game) | ? / uncertain | Track where each player played. **Value unclear** — positions change constantly within a game, so a single per-game position may not be meaningful; would likely need time-segmented tracking to be useful. Captured 2026-06-24; revisit before committing. |

---

## 🔵 P4 — Big bets (need planning before any code)

- [ ] **Playing-Time Fairness & Multi-Game Sub Planner** 🚧 *planning in progress* — the big one. Fair playing time as **share of available time**, balanced across **all contexts** (game→tournament→season→competition): post-game minute logging → per-context running balance → a planner that allocates subs across a *set* of upcoming games (using the already-played share as the base) to even out the end result. **Absorbs the Tournament Planner (#369) and #381** (substitution who-came-off). Phased: capture+ledger → single-game planner → multi-game parallel optimization. Plan: `playing-time-fairness-and-planner.md`.
- [ ] ~~**Tournament Planner — full replan** (#369)~~ — folded into "Playing-Time Fairness & Multi-Game Sub Planner" above (it's the forward/planner half). Old code at tag `archive/planner-integration`.
- [ ] **Desktop responsive UI** (#360) — phone-frame / desktop layout. To be rebuilt from scratch; clever ideas preserved at tag `archive/desktop-responsive-modals`.
- [ ] **AI Assistant (chat with your data)** — long-term flagship. LLM function-calling over the user's games/players/stats. **Prerequisite:** richer data collection first. (Premium-shaped, but billing is parked.)
- [ ] **Timer-hardening refactor** — make the match clock a single source of truth (period "segments" with wall-clock start/stop timestamps; derive elapsed/period/status). Would end the recurring reload/background clock bugs (CR-C1, background-pause) **and** unlock a real **timed-OT clock** (the one piece the overtime feature deliberately deferred). Only worth doing if/when an in-app extra-time clock is wanted; the shipped OT/penalties feature needs none of it.

---

## 🧪 Post-launch QA (verification pass, not features)

Manual checklists `TESTING-PLAN.md` (root) + `user-flow-testing-plan.md` are unexecuted — run as a verification sweep when convenient. Specific gaps to confirm: session-expiry handling, auth-init timeout recovery, post-login marketing-consent prompt. ⚠️ The "Free Limits" test is stale (premium enforcement is off / app is free).

---

## 📁 Reference

| Purpose | Location |
|---------|----------|
| This roadmap | `docs/03-active-plans/UNIFIED-ROADMAP.md` |
| Post-launch QA checklists | `docs/03-active-plans/user-flow-testing-plan.md`, `TESTING-PLAN.md` |
| Speculative feature ideas | `docs/04-features/future-vision.md` |
| Supabase implementation reference | `docs/02-technical/supabase-implementation-guide.md` |
| Archived plans (Play Store, billing, reviews, etc.) | `docs/08-archived/completed-active-plans/` |
| Preserved code snapshots | tags `archive/pre-cloud-backup`, `archive/planner-integration`, `archive/desktop-responsive-modals` |
| Open issues | #371 (consent bug), #381 (sub stats), #364, #360, #273, #369 (planner replan) |

---

## ✅ Completed (history)

- **Production launch** (2026-06-09) — Google Play, cloud-only gate enabled, free.
- **Supabase cloud backend** — DataStore abstraction, SupabaseDataStore, both-way migration, local-first sync (SyncQueue/SyncEngine/SyncedDataStore).
- **Billing infrastructure built** (Phases 1-7) — then parked (free); plan archived for an eventual replan.
- **Welcome-screen simplification**, **communication infrastructure** (domains, email routing, Sentry alerts, velomoai.com).
- **Features** — Gender + Game Type labeling/filtering, Personnel Management, Tournament Series & Season Leagues (34 Finnish leagues), First Game Onboarding, External Match Stats, Warm-up Plan, auto-save, backup/restore.
- **Platform** — Next.js 16 + React 19, 0 npm vulns, HomePage refactor (3,725→62 lines), PWA stability, IndexedDB migration.
- **Verified-fixed during triage** — 3 cloud-sync bugs (warmup ID conflict, warmup metadata, games `created_at`) were already fixed (commits `847f0dab`, `b0816143`); CORS extraction, TournamentSeriesManager extraction, jest-axe types all already done.

---

## 📝 Change Log

| Date | Update |
|------|--------|
| 2026-06-11 | 🧨 **Whole-app code review (7-agent) → P0.** 4 critical (mid-game reload clock corruption, IndexedDB read-error wipe path, cloud entity-delete FK failure, stale-snapshot re-migration), 9 high, 8 medium clusters added as CR-C/H/M items. Also: account-deletion hardening shipped (`77ac8034`), Play badge links live (`944509fe`), P2 items added for Play Store data-deletion declaration + local device-data wipe. |
| 2026-06-10 | 🧹 **Full backlog triage + prioritization.** Parked monetization (going free → closed #171/#172, archived billing plan). Scrapped Planner impl (closed PR #404 + #372/#373/#377/#378/#379, reframed #369, tagged code). Verified-and-discarded 3 phantom cloud-sync bugs + several already-done tech-debt items. Kept #371 (consent), #381, #364, #360, #273. Tagged & deleted desktop branch. Reprioritized everything P0-P4. |
| 2026-06-09 | 🚀 Production release — live on Google Play (cloud-only, free). |
| 2026-01-28 | Docs audit; future-vision features + cloud-sync bugs added to backlog. |
| 2025-12 | Supabase backend, Gender/Game Type features, Next.js 16 + React 19 upgrade. |

---

**Current Focus**: 🔴 P0 — code-review criticals CR-C1…C4 first (data loss / broken deletes), then CR-H items; store-listing accuracy, internal-docs publishing, and consent `status` bug (#371) remain alongside.
