/**
 * Instrumentation file for the marketing site
 *
 * This empty file prevents Next.js from picking up the root-level
 * instrumentation.ts which imports Sentry (not used in the marketing site).
 */

export async function register() {
  // No instrumentation needed for marketing site
}
